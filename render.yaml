databases:
  - name: localbizvault-db
    plan: free

services:
  - type: web
    name: localbizvault
    env: python
    pythonVersion: "3.11.0"
    buildCommand: pip install -r requirements.txt
    startCommand: python force_fix.py && gunicorn "app:create_app()"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: localbizvault-db
          property: connectionString
      - key: PYTHON_VERSION
        value: "3.11.0"